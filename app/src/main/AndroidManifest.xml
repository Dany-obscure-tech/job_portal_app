<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daniyalfarid.jobportal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo123"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo123"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".post_activity" /> <!-- <activity android:name=".TestAvtivity" /> -->
        <activity android:name=".AddJobActivity" />
        <activity android:name=".JobApplicationForm" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".ThirdActivity" />
    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>